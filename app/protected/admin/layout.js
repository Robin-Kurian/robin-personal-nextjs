export const viewport = {
    width: 'device-width',
    initialScale: 1,
    maximumScale: 1,
};

export const metadata = {
    title: 'Admin Dashboard',
    description: 'Administrative dashboard for managing Baby Paradise e-commerce platform, including orders, products, and settings.',
};

export default function AdminLayout({ children }) {
    return children;
} 